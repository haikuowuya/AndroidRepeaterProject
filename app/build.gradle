apply plugin: 'com.android.application'
android {
    compileSdkVersion 14
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "fq.router2"
        minSdkVersion 9
        targetSdkVersion 14
    }

    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
    compile files('/Users/Kingpin/Documents/AndroidDoodle/fqrouter-studio/libs/android-support-v4.jar')
}